int a = 3;
int main(int b);
int fun();
int outra(int x, int y);

code main {
    a = input(int);
    while(a > (2 + 3)) {
        print "Ok";
        a = outra(a, -2);
    }

    return 34;
}

code fun {
}

code outra 
{
    return x + y;
}

